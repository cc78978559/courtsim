# ADR-0001：本地优先工具链

- 状态：接受
- 日期：2026-07-22

## 决定

CourtSim以Windows本地Python进程作为默认运行环境，运行时不要求网络服务或第三方Python包。开发依赖放在项目虚拟环境，并使用锁定文件与本地wheelhouse重建。

原始事件保留为JSONL，运行摘要和来源清单保留为JSON。SQLite等分析组件只有在批量数据规模证明有需要时才引入。

## 原因

模拟器需要可重复批跑、便携调试和长期回归验证。减少运行时依赖可以降低环境漂移，并让同一配置和种子更容易复现。

